apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "meos-web.fullname" . }}-test-connection"
  labels:
    {{- include "meos-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "meos-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
